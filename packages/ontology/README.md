# @control-panel-erp/ontology

Ontology Language package — entity types, links, actions → taxonomy skills, plus connector bindings (ACL-side).

```bash
npm test -w @control-panel-erp/ontology
```

Public catalog helpers omit vendor bindings. See [docs/Development/ontology.md](../../docs/Development/ontology.md).
