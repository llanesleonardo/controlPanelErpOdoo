# Ontology catalog API + UI — conops

Operator opens Ontology, sees Estimate and its actions, then jumps to ERP Map / section intents to run skills. Agents may later call the same catalog for tool discovery.
