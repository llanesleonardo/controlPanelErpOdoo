# Frontend and UI

| Pattern | Definition |
| --- | --- |
| MVC | Separates an application into Model. |
| MVVM | Separates the View from presentation state and. |
| MVP | Separates presentation logic into a Presenter. |
| Flux | Uses one-way data flow where actions go through. |
| Redux | Centralizes application state in a store and. |
| Component-Based Architecture | Component-Based Architecture builds UI from reusable. |
| Micro Frontends | Split a frontend into independently owned and. |
| Observer | Lets objects subscribe to changes from a subject. |
| State Container | Centralizes shared state and provides controlled. |
| Virtual DOM | Represents UI as an in-memory tree and. |
