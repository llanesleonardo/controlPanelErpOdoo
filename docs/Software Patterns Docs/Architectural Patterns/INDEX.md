# Architecture Patterns

| Pattern | Definition |
| --- | --- |
| Client-Server Architecture | Client-Server separates systems into clients. |
| Microservices Architecture | Microservices split a system into independently. |
| SOA | Organizes enterprise systems around reusable. |
| Event-Driven Architecture | Event-Driven Architecture uses events to notify. |
| Hexagonal Architecture | Isolates the application core from external. |
| Clean Architecture | Places business rules at the center and makes. |
| Onion Architecture | Organizes systems in concentric layers around. |
| Pipe and Filter Architecture | Pipe and Filter breaks processing into. |
| Broker Architecture | Uses a broker to coordinate communication. |
| Peer-to-Peer Architecture | Peer-to-Peer Architecture connects nodes that. |
| CQRS | Separates write operations that change state. |
| Event Sourcing | Stores changes as a sequence of events instead. |
| Serverless Architecture | Runs application logic in managed functions or. |
| Space-Based Architecture | Space-Based Architecture distributes processing. |
| Reactive Architecture | Designs systems to be responsive. |
| Actor Model | Structures a system as independent actors that. |
| Blackboard Architecture | Uses a shared knowledge base where independent. |
| MVC | Separates an application into Model. |
| MVVM | Separates UI from presentation state and. |
| MVP | Separates presentation logic into a Presenter. |
| Backend-for-Frontend | BFF creates a backend tailored to the needs of a. |
| Layered Architecture | Organizes software into separate layers. |
| Modular Monolith Architecture | Modular Monolith is an architectural style where. |
| Monolith Architecture | Monolith is a software architecture where the. |
