# Messaging and Integration

| Pattern | Definition |
| --- | --- |
| Publish-Subscribe | Publish-Subscribe lets one producer publish a. |
| Queue | Stores messages until consumers are ready to. |
| Producer-Consumer | Producer-Consumer separates components that. |
| Request-Reply | Request-Reply sends a request message and. |
| Message Broker | Receives messages from producers. |
| Message Bus | Provides a shared communication backbone where. |
| Dead Letter Queue | Stores messages that cannot be delivered or. |
| Competing Consumers | Use multiple consumers reading from the same queue. |
| Event Streaming | Stores ordered streams of events that consumers. |
| Event Notification | Sends a lightweight message that something happened. |
| Event-Carried State Transfer | Event-Carried State Transfer includes enough. |
| Content-Based Router | Content-Based Router routes messages to. |
| Message Filter | Removes messages that do not meet specified. |
| Aggregator | Collects related messages and combines them into. |
| Splitter | Breaks one complex message into multiple smaller. |
| Resequencer | Reorders messages that arrive out of sequence. |
| Message Translator | Converts one message format. |
| Canonical Data Model | Defines a shared enterprise message format. |
| Correlation Identifier | Is an ID included in related messages. |
| Message Store | Persists messages for auditing. |
| Claim Check | Stores large message payloads externally and. |
| Pipes and Filters | Decomposes message processing into independent. |
